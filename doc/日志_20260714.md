# 开发日志 — 2026-07-14

## 项目
鸿蒙App + ROS2 小车 — SLAM建图 + 导航测试

---

## 今日完成

### 1. 小车SLAM服务部署 ✅
- 远程SSH连接小车 (`10.168.202.242`)
- 启动 RPLIDAR（通用型号，115200 baud） + gmapping + slam_nav_service（端口7000）
- 修复：RPLIDAR 原用 A3 模板(256000 baud)错误，改为通用 `sllidar_launch.py`(115200 baud)
- 修复：CORS 跨域支持（`@app.after_request` + `@app.before_request` 处理 OPTIONS 预检）
- 修复：多处同时运行多个 slam_nav_service 实例的问题

### 2. 操控网页 ✅ 
- 创建 `car_control.html` — Windows 浏览器直接操控小车
- 功能：D-pad方向控制 + 速度滑块 + WASD键盘 + 实时位置显示
- 方案C闭环：开始构建 → 遥控 → 记录航点 → 结束保存

### 3. Bug修复
- **`build/end` 保存失败**: 文件名含斜杠 `2026/7/14` 被当目录路径 → 两端均加 sanitize
- **同名路线覆盖**: 加入重复检查（客户端confirm + 服务端 `force` 参数）
- **必须输入名字才能保存**: 空名时toast提示，不自动生成
- **键盘输入干扰**: 输入框聚焦时忽略键盘事件

### 4. 导航测试
- 测试了 `ditu1`（A1, B1）和 `luxian12`（3个航点）
- **路线已跳过原点** ✅（只导航到waypoints）
- **卡死检测已改为SLAM位置** ✅（之前用编码器，麦轮转向误触发）

### 5. 诊断结论
- 导航定位跳变：SLAM 2-5Hz低频 + 开旷环境下扫描匹配不稳定 + 麦轮打滑
- **计划修复**: 将 `navigate_to` 改为混合定位（IMU转向 + 编码器走直线 + SLAM终点校验），参考 `move_dir` 模式

---

## 当前服务状态

| 服务 | 端口 | 状态 |
|------|------|------|
| RPLIDAR | `/dev/rplidar` | ✅ 10Hz |
| gmapping | ROS2 | ✅ 建图中 |
| slam_nav_service | 7000 | ✅ 已启动 |

## 操控网页
`C:\Users\21774\Desktop\car_control.html`

## 已知问题
1. `navigate_to` 还需改为混合定位模式（TODO）
2. SLAM 重启后坐标系重置，旧航点坐标失效 — 需要地图持久化
3. 串口 `/dev/myserial` 被 app.py 抢占时 slam_nav_service 会冲突
